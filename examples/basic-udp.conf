# Basic UDP Multicast Configuration
# Use for local network logging with minimal overhead

[Network]
# Multicast address and port
Address=239.0.0.1:6000

# Protocol (udp is default)
Protocol=udp

# Log format (rfc5424 is default)
LogFormat=rfc5424
